<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>TX-{{bill.bill.bill_number}} by {{author.name}}</title>

    <meta name="twitter:card" content="summary">
    <meta name="og:title" content="TX-{{bill.bill.bill_number}} by {{author.name}}">
    <meta name="twitter:title" content="{{bill.bill.bill_number}} by {{author.name}} ({{author.party}})">
    <meta name="twitter:description" content="{{bill.bill.description}}">
    <meta name="og:description" content="{{bill.bill.description}}">
    {{#districtUrl}}
    <meta name="twitter:image" content="{{.}}">
    <meta name="og:image" content="{{.}}">
    {{/districtUrl}}
    
    {{>head}}

    </style>
</head>
<body>
    {{>nav}}
    <div class="container">
    <div class="bill">
        <h1>TX-{{bill.bill.bill_number}}</h1>
        <h4>{{bill.bill.session.session_name}}</h4>
        <p class="bill_text">{{bill.bill.description}}</p>
    </div>
    <div class="cta">
        
        <a id="learnMore" href="{{bill.bill.state_link}}" class="btn btn-lg btn-primary">Learn More About {{bill.bill.bill_number}}</a>
    </div><br/>
    <div class="author">
    <h2>{{author.name}}</h2>
    <h4>Author of {{bill.bill.bill_number}}, {{#party author.party}}{{/party}} from {{author.district}}</h4>
    {{#districtUrl}}
    <img class="district_map" src="{{.}}">
    {{/districtUrl}}
    <h2>Learn More about {{author.name}}</h2>
    {{#author.votesmart_id}}
        <a href="http://votesmart.org/candidate/{{.}}" class="btn btn-lg">{{../author.name}} at VoteSmart</a>
    {{/author.votesmart_id}}
    {{#author.ballotpedia}}
        <a href="https://ballotpedia.org/{{.}}" class="btn btn-lg ">{{../author.name}} at Ballotpedia</a>
    {{/author.ballotpedia}}
    {{#author.ftm_eid}}
        <a href="https://followthemoney.org/entity-details?eid={{.}}&default=candidate" class="btn btn-lg">{{../author.name}} at FollowTheMoney</a>
    {{/author.ftm_eid}}
    </div>
    </div>
    {{>footer}}
</body>
</html>